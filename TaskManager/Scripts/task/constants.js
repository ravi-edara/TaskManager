var constants = {
    url: {
        apiHome: 'api/home',
        apiGetTaskById: 'api/home/{0}'
    },
    controller: {
        taskCtrl: 'taskCtrl'
    },
    format: {
        dateFormat: 'DD MMM YYYY'
    }
}